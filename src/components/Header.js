import React from "react";

const Header = () => {

    return (

        <div className="header-container">
            <p class="header-text">Welcome to the Chat Arena !</p>
        </div>

    );
}

export default Header;